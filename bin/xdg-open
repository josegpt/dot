#!/bin/sh

. utils

case "${1%%:*}" in
	http|https)
		if [ -z "$DISPLAY" ]
		then
			exec lynx "$1"
		else
			exec firefox --new-tab "$1" &
		fi
	;;
	*.jpg)
		exec feh "$1"
	;;
	*)
		_die "I do not how to handle $@"
	;;
esac
